import { findWinnerByDate } from "@/lib/repositories/catalog";

export async function GET(_: Request, { params }: { params: Promise<{ winnerId: string }> }) { const { winnerId } = await params; const winner = await findWinnerByDate(winnerId); if (!winner) return new Response("Not found", { status: 404 }); const escape = (value: string) => value.replace(/[&<>"']/g, (character) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&apos;" })[character] ?? character); const svg = `<svg xmlns="http://www.w3.org/2000/svg" width="720" height="220" viewBox="0 0 720 220"><rect width="720" height="220" rx="24" fill="#14212b"/><circle cx="112" cy="110" r="57" fill="#ff6b4a"/><path d="M112 72l10 22 24 3-18 16 5 24-21-12-21 12 5-24-18-16 24-3z" fill="white"/><text x="200" y="88" font-family="Arial,sans-serif" font-size="18" font-weight="700" fill="#ff6b4a">#1 ON LAUNCH BRAWL</text><text x="200" y="125" font-family="Arial,sans-serif" font-size="32" font-weight="800" fill="white">${escape(winner.productName)}</text><text x="200" y="158" font-family="Arial,sans-serif" font-size="15" fill="#aeb7bc">${escape(winner.date)} · Verified daily winner</text></svg>`; return new Response(svg, { headers: { "Content-Type": "image/svg+xml", "Cache-Control": "public, max-age=3600" } }); }
