import { ArrowUpRight, CheckCircle2, ShieldCheck, Users } from "lucide-react";
import { PageContainer } from "@/components/layout/PageContainer";
import { SubmitForm } from "@/components/submit/SubmitForm";
import { listCategories } from "@/lib/repositories/catalog";

export const metadata = { title: "Submit a product" };

export default async function SubmitPage() { const categories = await listCategories(); return <PageContainer><div className="grid gap-12 lg:grid-cols-[.78fr_1.22fr] lg:gap-20"><div><div className="eyebrow text-coral">Put it on the board</div><h1 className="display mt-4 text-5xl font-black leading-[.98] tracking-tight sm:text-7xl">Your thing belongs in the mix.</h1><p className="mt-6 max-w-md text-base leading-7 text-muted">Launch Brawl is free to list. Give your product a home, let the community find it, and decide later if you want to compete for paid reach.</p><div className="mt-10 grid gap-5">{[{ icon: CheckCircle2, title: "Free product profile", text: "Name, story, category, maker attribution, and a clean shareable page." }, { icon: Users, title: "Community discovery", text: "Votes, favorites, brawls, and organic directories keep the ecosystem open." }, { icon: ShieldCheck, title: "Human review", text: "Every listing has a moderation path before it appears publicly." }].map((item) => <div key={item.title} className="flex gap-3"><item.icon size={19} className="mt-0.5 text-coral" /><div><p className="text-sm font-bold">{item.title}</p><p className="mt-1 text-sm leading-6 text-muted">{item.text}</p></div></div>)}</div><a href="/pricing" className="mt-9 inline-flex items-center gap-2 text-sm font-bold text-ink underline decoration-coral decoration-2 underline-offset-4">See the reach model <ArrowUpRight size={15} /></a></div><SubmitForm categories={categories} /></div></PageContainer>; }
